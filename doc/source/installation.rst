============
Installation
============

At the command line::

    $ pip install python-oscplugin

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv python-oscplugin
    $ pip install python-oscplugin